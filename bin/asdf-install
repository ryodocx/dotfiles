#!/usr/bin/env bash
set -ex

toolName=$1
asdf plugin-add ${toolName} || :
version="$(asdf list-all ${toolName} | grep-by-stable-version | sort-by-version -r | head -n 1)"
asdf install ${toolName} ${version}
asdf global ${toolName} ${version}
